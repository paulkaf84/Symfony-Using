<html lang="en">
<head>
    <title>Form test page</title>
</head>
<body>
    {{ form_start(form) }}
        {{ form_row(form.title) }}
        {{ form_row(form.content) }}
        {{ form_row(form.author) }}
        <button type="submit">Send</button>
    {{ form_end(form) }}
</body>
</html>